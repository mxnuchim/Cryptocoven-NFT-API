{
  "name": "xcryptocoven",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ mxnuchim/xcryptocoven",
    "create-local": "graph create --node http://localhost:8020/ mxnuchim/xcryptocoven",
    "remove-local": "graph remove --node http://localhost:8020/ mxnuchim/xcryptocoven",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mxnuchim/xcryptocoven"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
